# Generated by Cargo
# will have compiled files and executables
target/
**/*.rs.bk

# Remove Cargo.lock from gitignore if creating an executable, leave it for libraries
# More information here https://doc.rust-lang.org/cargo/guide/cargo-toml-vs-cargo-lock.html
# Cargo.lock (commented out - executables should track lock file)

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb
*.ilk
*.exp

# Temporary files created during builds
*.tmp
*.temp

# IDEs and editors
.vscode/
.idea/
*.swp
*.swo
*~
*.sublime-*
.vim/

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini
$RECYCLE.BIN/

# Testing and coverage
coverage/
*.profraw
tarpaulin-report.html

# Logs
*.log
logs/

# Editor artifacts
*.code-workspace
.project
.classpath
.settings/

# Temporary files
tmp/
temp/
*.cache

# Build artifacts (non-Cargo)
build/
dist/
out/

# Configuration files that might contain sensitive information
.env
.env.local
.env.*.local
*.secret
*.key
*.pem
*.pfx
*.p12
*.cert
*.crt
*.der
!**/test*.pem
!.github/**/*.pem

# Application data files that might contain sensitive information
history.json
*.history
config.json
secrets.json
identity.json
keystore.json
*.db
*.sqlite

# Private keys and certificates (CRITICAL - never commit these!)
private_key.pem
id_rsa
id_rsa.pub
*.priv
*.private

# Build artifacts that might contain embedded secrets
*.exe
!target/release/*.exe
*.dll
*.so
*.dylib

# Dependency directories (non-Rust)
node_modules/
venv/
__pycache__/

# Distribution / packaging (keep .exe for installer generation)
*.deb
*.dmg
*.msi
*.rpm
*.tar.gz
*.zip
!**/target/release/*.exe

# Rust-specific
**/*.orig
*.rej

# Windows installer outputs
*.iss.bak
!setup.iss

# Debug symbols (optional - uncomment if you want to ignore)
# *.dSYM/
# *.pdb

# Documentation organization: keep archived originals tracked by git
!docs/
!docs/archived/
!docs/archived/*.md.orig